import { expect, test, describe } from 'vitest'

describe('test pull request action', () => {
    test('Should pull request have a information about new action', () => {
        expect(true).toBe(true)
    })
})